---
description: Convert the generated tasks into GitHub issues
argument-hint: "[input passed to the speckit-taskstoissues skill]"
---

Invoke the `speckit-taskstoissues` skill (`.claude/skills/speckit-taskstoissues/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.taskstoissues form works alongside the /speckit-taskstoissues skill. -->
