---
description: Assess an existing codebase against spec artifacts and append remaining work as tasks
argument-hint: "[input passed to the speckit-converge skill]"
---

Invoke the `speckit-converge` skill (`.claude/skills/speckit-converge/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.converge form works alongside the /speckit-converge skill. -->
