---
description: Execute the task list and build the feature
argument-hint: "[input passed to the speckit-implement skill]"
---

Invoke the `speckit-implement` skill (`.claude/skills/speckit-implement/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.implement form works alongside the /speckit-implement skill. -->
