---
description: Cross-artifact consistency and alignment report (read-only)
argument-hint: "[input passed to the speckit-analyze skill]"
---

Invoke the `speckit-analyze` skill (`.claude/skills/speckit-analyze/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.analyze form works alongside the /speckit-analyze skill. -->
