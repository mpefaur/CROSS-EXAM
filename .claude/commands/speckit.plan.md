---
description: Create the technical implementation plan for the current feature
argument-hint: "[input passed to the speckit-plan skill]"
---

Invoke the `speckit-plan` skill (`.claude/skills/speckit-plan/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.plan form works alongside the /speckit-plan skill. -->
