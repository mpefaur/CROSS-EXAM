---
description: Create or update the feature specification from a natural language description
argument-hint: "[input passed to the speckit-specify skill]"
---

Invoke the `speckit-specify` skill (`.claude/skills/speckit-specify/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.specify form works alongside the /speckit-specify skill. -->
