---
description: Generate quality checklists validating requirement completeness and clarity
argument-hint: "[input passed to the speckit-checklist skill]"
---

Invoke the `speckit-checklist` skill (`.claude/skills/speckit-checklist/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.checklist form works alongside the /speckit-checklist skill. -->
