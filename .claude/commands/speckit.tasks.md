---
description: Generate an actionable, dependency-ordered task list from the plan
argument-hint: "[input passed to the speckit-tasks skill]"
---

Invoke the `speckit-tasks` skill (`.claude/skills/speckit-tasks/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.tasks form works alongside the /speckit-tasks skill. -->
