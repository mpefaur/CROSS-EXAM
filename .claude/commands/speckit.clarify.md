---
description: Ask structured questions to de-risk ambiguous areas of the spec before planning
argument-hint: "[input passed to the speckit-clarify skill]"
---

Invoke the `speckit-clarify` skill (`.claude/skills/speckit-clarify/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.clarify form works alongside the /speckit-clarify skill. -->
