---
description: Establish or update the project constitution (.specify/memory/constitution.md)
argument-hint: "[input passed to the speckit-constitution skill]"
---

Invoke the `speckit-constitution` skill (`.claude/skills/speckit-constitution/SKILL.md`) and follow its
instructions exactly, using the following as its input:

$ARGUMENTS

<!-- Alias so the documented /speckit.constitution form works alongside the /speckit-constitution skill. -->
